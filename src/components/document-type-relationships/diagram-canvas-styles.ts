/** Fondo punteado del canvas de /diagrams, para los estados que se muestran sin `<ReactFlow>` montado. Debe coincidir con el `<Background>` real. */
export const DOTTED_CANVAS_STYLE = {
  backgroundColor: "var(--surface-sunken)",
  backgroundImage: "radial-gradient(#d5dce5 1px, transparent 1px)",
  backgroundSize: "22px 22px",
} as const
