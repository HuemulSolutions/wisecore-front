/** Fondo punteado del canvas de /diagrams, para los estados que se muestran sin `<ReactFlow>` montado. */
export const DOTTED_CANVAS_STYLE = {
  backgroundColor: "#fafbfc",
  backgroundImage: "radial-gradient(#dfe3e9 1px, transparent 1px)",
  backgroundSize: "22px 22px",
} as const
