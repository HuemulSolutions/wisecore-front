/**
 * Constantes de tipo para los dos nodos inline void insertados por el combobox de
 * mención `@` (ver `reference-kit.tsx`). Mismo molde que `MERMAID_KEY` en
 * `plate-mermaid-utils.ts` — un nodo custom fuera de la librería necesita su propia
 * constante de `type` para registrarse como plugin y como regla de Markdown.
 */
export const ASSET_REFERENCE_KEY = 'asset_reference';
export const ROLE_REFERENCE_KEY = 'role_reference';
